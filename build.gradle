buildscript {
  repositories {
    jcenter()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:2.0.0'
  }
}

allprojects {
  group = 'com.salesforce.androidsdk'
  
  repositories {
    jcenter()
  }
}