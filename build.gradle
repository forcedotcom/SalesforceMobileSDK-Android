buildscript {
  repositories {
    jcenter()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:1.5.0'
  }
}

allprojects {
  group = 'com.salesforce.androidsdk'
  
  repositories {
    jcenter()
  }
}