buildscript {
  repositories {
    jcenter()
  }

  dependencies {
    classpath 'com.android.tools.build:gradle:2.1.2'
  }
}

allprojects {
  group = 'com.salesforce.androidsdk'
  
  repositories {
    jcenter()
  }
}